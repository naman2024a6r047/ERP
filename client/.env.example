# When running client+server on the same domain, use a relative path:
REACT_APP_API_URL=/api

# When running on separate domains, use the full URL:
# REACT_APP_API_URL=https://api-erp.kishtwareduhub.com/api
